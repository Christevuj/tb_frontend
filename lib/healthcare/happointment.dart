import 'package:flutter/material.dart';

class Happointment extends StatelessWidget {
  const Happointment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Healthcare Appointments Page'),
    );
  }
}
