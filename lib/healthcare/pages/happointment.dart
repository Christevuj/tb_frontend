import 'package:flutter/material.dart';

class HAppointment extends StatelessWidget {
  const HAppointment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Healthcare Worker Appointments'),
      ),
    );
  }
}
