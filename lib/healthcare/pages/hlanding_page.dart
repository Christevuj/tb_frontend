import 'package:flutter/material.dart';

class HLandingPage extends StatelessWidget {
  const HLandingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Healthcare Worker Home'),
      ),
    );
  }
}
