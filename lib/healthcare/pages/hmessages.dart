import 'package:flutter/material.dart';

class HMessages extends StatelessWidget {
  const HMessages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Healthcare Worker Messages'),
      ),
    );
  }
}
