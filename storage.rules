rules_version = '2';

// Firebase Storage security rules for TBisita chat images
service firebase.storage {
  match /b/{bucket}/o {
    // Allow full access for authenticated users (testing)
    match /{allPaths=**} {
      allow read, write: if true;  // Completely open for now
    }
  }
}